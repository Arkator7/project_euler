import { expect } from 'chai';

import * as euler072 from './../src/euler072';

describe('Euler072 Tests', () => {
  describe('Testing function()', () => {
    it('for various numbers', done => {
      expect(true).to.equal(true);

      done();
    });
  });
});